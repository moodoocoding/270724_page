# `codex/lesson1-design-flow` 브랜치 안내

## 생성 이유

1차시 ‘문제 정의’ 화면을 크게 바꾸는 작업을 `main`에서 바로 진행하면 현재 연수용 서비스의 안정적인 버전을 되돌리기 어렵습니다. 새 구성을 충분히 검토한 뒤 선택적으로 병합할 수 있도록 별도 브랜치를 만들었습니다.

- 브랜치: `codex/lesson1-design-flow`
- 분기 기준 원격 커밋: `565c481` (`origin/main`)
- 기준 문서: [AI 활용 수업 설계 질문지_1차시 예시답안](https://docs.google.com/document/d/1ZPo_eackRFEIyRdix1KjQh8cnlVy5Ikq98bwC1UqTK4/edit)
- 작성일: 2026-08-04

## 이 브랜치에서 변경한 내용

기존 한 화면짜리 1차시를 다음 다섯 개의 내부 단계로 분리했습니다.

1. 남길 배움(A)
2. 현재 장면(B): 사실, 가능한 해석, 아직 모르는 것과 다음 확인
3. 배움 확인 기준(C)
4. 수업 문제 문장
5. AI 활용 결정(D)

화면 상단의 단계 탭과 하단의 이전·다음 버튼으로 이동합니다. 한 번에 한 단계만 보여 주어 작성 부담과 화면 밀도를 낮췄습니다.

추가로 다음 사항을 함께 반영했습니다.

- 기존 1차시 작성 데이터는 새 화면의 관련 입력란에 가능한 범위에서 다시 표시
- 새 입력값이 Supabase의 `submissions.data_json`에 저장되도록 API 허용 키 확장
- 완성한 수업 문제 문장을 2차시 ‘AI에게 요청하기’ 출발 문장에 자동 연결
- 강사 화면과 CSV 내려받기에 새 1차시 결과 반영
- 작성하지 않은 항목이 있어도 기존처럼 임시 저장 및 제출 가능
- 데스크톱·태블릿·모바일 반응형 레이아웃 적용

## 이번 브랜치에서 바꾸지 않은 것

- Supabase 테이블 구조
- 기존 참가자 로그인 및 세션 방식
- 2·3·4차시의 기본 활동 구조
- `main` 브랜치와 배포 환경

## 검증

- `npm.cmd run lint`
- `npm.cmd run build`
- 로컬 브라우저에서 단계 전환, 오류 오버레이, 콘솔 오류, 가로 넘침 확인

## 병합 전 확인할 항목

- 실제 연수 진행 순서와 내부 5단계 명칭이 맞는지
- 한 화면에서 요구하는 작성량이 적절한지
- 기존 참가자의 1차시 데이터가 의도한 입력란에 표시되는지
- 1차시 제출 후 2차시 출발 문장이 자연스러운지

